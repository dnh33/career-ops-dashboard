---
interface Props {
  name?: string;
  initials?: string;
  class?: string;
}

const {
  name = 'Daniel',
  initials = 'D',
  class: className = '',
} = Astro.props;
---

<div class:list={['user-avatar', className]} title={name} aria-label={name}>
  <span class="user-avatar__initials">{initials}</span>
</div>

<style>
  .user-avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: var(--size-8);
    height: var(--size-8);
    border-radius: var(--radius-full);
    background: color-mix(in srgb, var(--accent-primary) 12%, transparent);
    border: var(--border-width) solid var(--border-default);
    flex-shrink: 0;
  }

  .user-avatar__initials {
    font-size: var(--text-xs);
    font-weight: 600;
    font-family: var(--font-sans);
    color: var(--accent-primary);
    line-height: 1;
  }
</style>